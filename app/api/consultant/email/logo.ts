// Re-exported from the shared location so other email routes (readiness,
// consultant, and any future ones) can use the same logo without duplicating
// the base64 string in the repo.
export { PHAROS_LOGO_BASE64 } from "@/lib/emailLogo";
