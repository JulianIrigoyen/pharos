// Deprecated: Word Formation now renders through TextInputPart.tsx —
// an answer sheet only needs one text field per question (the student's
// derived word), not a place to retype the given word (that's on the PDF).
// This file is no longer imported anywhere and is safe to delete.
export {};
