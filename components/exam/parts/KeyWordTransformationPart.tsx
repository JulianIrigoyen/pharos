// Deprecated: Key Word Transformations now renders through
// TextInputPart.tsx — an answer sheet only needs one text field per
// question (the student's completed sentence), not places to retype the
// original sentence and key word (those are on the PDF).
// This file is no longer imported anywhere and is safe to delete.
export {};
